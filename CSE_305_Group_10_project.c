///program_skeleton
#include<stdio.h>

///int Inversion_pair(parameters)
   ///find the no of inversion pairs of an element


///int Find_key(parameters)
   ///selects the element with most inversion pairs

///void New_array(parameters)
   ///the selected and inversion paired elements will be eliminated
   ///the process will continue
   ///until no element left in the array
int main()
{
    ///scanf(test_case);
    do{
        ///scan no_of_objects
        ///scan the elements
        ///new_array_function

    }while(!test_case);
}
